export interface IDialog {
    id: string;
    timestamp: string;
    dialogId: string;
    senderId: string;
    receiverId: string;
    text: string;
    senderLogin: string;
    receiverLogin: string;
}